<%= form_with(model: [commentable, comment], local: false) do |form| %>
  <%= form.text_area :content, rows:'5', placeholder: Comment.human_attribute_name(:content), id: "comment_textarea" %>
  <%= form.submit %>
<% end %>
